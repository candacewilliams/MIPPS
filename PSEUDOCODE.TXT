READ IN STRING
CHECK IF STRING HAS NO MORE THAN 8 CHARACTERS
	IF YES, DISPLAY INVALID HEXADECIMAL NUMBER
        IF NO, CHECK TO SEE IF STRING HAS ALL VALID CHARACTERS
		IF NOT, DISPLAY INVALID HEXADECIMAL NUMBER
		IF YES, CHECK TO SEE IF STRING IS EMPTY
			IF YES DISPLAY SOME KIND OF EXCEPTION?
			IF NOT, FIND DECIMAL OF EACH CHARACTER ON THE ASCII TABLE
				THEN MAKE THAT NUMBER INTO IT'S BASE 10 FORM BY SUBTRACTING:
				87 FROM LOWER CASE
				55 FROM UPPER CASE
				48 FROM 0-9 NUMBER
